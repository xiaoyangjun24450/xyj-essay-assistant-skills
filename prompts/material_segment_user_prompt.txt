请分析下面这批用户素材的语义分段结果。

## 窗口信息
- 当前窗口: 第 {window_index} / {total_windows} 窗
- 单个 segment 长度上限: {max_segment_chars} 字

## 上一个窗口最后一个 segment
{previous_segment_json}

## 必要前后文
{context_hint}

## 当前窗口段落
{window_content}

要求：
- 按主题相关性分段，建议每个 segment 包含 1-5 个段落。
- 如果某个 segment 可能超长，优先在自然语义断点处分成多个 segment。
- 保持 para_ids 原始顺序，完整覆盖当前窗口全部段落。
- 如果提供了上一个窗口最后一个 segment，则当前窗口第一个 segment 必须先完整承接这些 para_ids。
- summary 要简洁准确，能概括该段素材主题。
- 只输出 JSON 数组，不要输出其他内容。

请直接输出 JSON 数组，格式如下：
[
  {{
    "segment_id": "mseg_temp_1",
    "summary": "本分段的简要概述",
    "purpose": "fact",
    "para_ids": ["1", "2"]
  }}
]
